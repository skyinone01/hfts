
(function () {
    'use strict';

    angular.module('BlurAdmin.pages.common', [])
        .config(routeConfig);

    /** @ngInject */
    function routeConfig($stateProvider, $urlRouterProvider) {

     }

})();
